---

buildpack: python_buildpack
command: unset GUNICORN_CMD_ARGS; ./startup-paas.sh gunicorn -c /home/vcap/app/gunicorn_config.py application

services:
  - tm-creds
  - tm-pg-service

env:
  FLASK_APP: application.py

instances: 1
memory: 512M

applications:
  - name: team-metrics
